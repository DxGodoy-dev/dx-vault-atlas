# DX Vault Atlas Configuration

# Required: Path to the vault root (for migration scanning)
DXVA_VAULT_PATH=/path/to/vault

# Required: Path to inbox folder (where new notes are created)
DXVA_VAULT_INBOX=/path/to/vault/inbox

# Optional: Editor command (defaults to notepad on Windows, vim on Unix)
DXVA_EDITOR=notepad
